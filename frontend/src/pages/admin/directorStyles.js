// =====================================
// ADMIN STYLES (alias)
// The Admin portal was created by adapting
// the director portal (same real-data pattern,
// same shared style module). The copied pages
// import a local "directorStyles.styles" — alias
// it back to the shared adminStyles module so
// the Admin portal stays visually consistent.
// =====================================

export { default } from "./adminStyles";
