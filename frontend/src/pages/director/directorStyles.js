// =====================================
// DIRECTOR STYLES
// Re-export the SAME shared styles used
// by every other role portal (mirrors
// teacher/teacherStyles.js) so the
// Director Portal stays visually
// consistent with the whole app.
// =====================================

export { default } from "../student/studentStyles";
