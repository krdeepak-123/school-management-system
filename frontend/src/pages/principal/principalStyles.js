// =====================================
// PRINCIPAL STYLES
// Reuse the same shared styles used by
// the other role portals (mirrors
// teacher/teacherStyles.js) so the
// Principal Portal stays consistent.
// =====================================

export { default } from "../student/studentStyles";
